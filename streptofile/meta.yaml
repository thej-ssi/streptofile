{% set name = "streptofile" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/streptofile-{{ version }}.tar.gz
  sha256: 1a368bf5aa251d12bbe36a96c86b9a43b52a1764d2dedc4670c459d4d3395fe9

build:
  entry_points:
    - streptofile = streptofile.run_streptofile:main
    - emm_typer = streptofile.emm_typer:main
    - mlstyper = streptofile.mlstyper:main
    - virulence_profiler = streptofile.virulence_profiler:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.14
    - uv-build
    - pip
  run:
    - python >=3.14
    - polars
    - python-dotenv
    - blast

test:
  imports:
    - streptofile
  commands:
    - pip check
    - streptofile --help
    - emm_typer --help
    - mlstyper --help
    - virulence_profiler --help
  requires:
    - pip

about:
  summary: EMM typing, MLST and Virulence gene detection for S. pyogenes whole-genome sequencing data
  license: MIT

extra:
  recipe-maintainers:
    - thej-ssi
